<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.eseo.dis.camille.pfeandroid">

    <uses-sdk
        android:maxSdkVersion="17"
        android:minSdkVersion="8" />

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".ListProjectActivity" />
        <activity android:name=".SideMenuActivity" />
        <activity android:name=".ProjectDetailsActivity" />
        <activity android:name=".ListJuryActivity" />
        <activity android:name=".JuryDetailsActivity" />
        <activity android:name=".DisplayPosterActivity" />
        <activity android:name=".EvaluationActivity" />
        <activity android:name=".EvaluationValidationActivity" />
        <activity android:name=".AddPseudoJuryActivity" />
        <activity android:name=".ShowPseudoJurysActivity"></activity>
    </application>

</manifest>